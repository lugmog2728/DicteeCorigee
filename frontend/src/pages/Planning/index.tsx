import Typography from '../../components/Typography'

export default function Planning() {
  return (
    <div className="flex flex-col gap-6">
      <div>
        <Typography variant="h1">Planification</Typography>
        <Typography variant="subtitle">Planifiez et gérez vos dictées pour vos classes</Typography>
      </div>
    </div>
  )
}
